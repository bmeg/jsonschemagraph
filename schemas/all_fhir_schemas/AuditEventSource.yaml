$id: AuditEventSource
additionalProperties: false
description: Audit Event Reporter. The actor that is reporting the event. [See https://hl7.org/fhir/R5/AuditEventSource.html]
links:
- href: Practitioner/{id}
  rel: observer_Practitioner
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: observer_PractitionerRole
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: observer_Organization
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: CareTeam/{id}
  rel: observer_CareTeam
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: CareTeam.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: observer_Patient
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: Device/{id}
  rel: observer_Device
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Device.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: observer_RelatedPerson
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /observer/reference
  templateRequired:
  - id
- href: Location/{id}
  rel: site
  targetHints:
    backref:
    - audit_event_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Location.yaml
  templatePointers:
    id: /site/reference
  templateRequired:
  - id
properties:
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  observer:
    $ref: Reference.yaml
    backref: audit_event_source
    description: Identifier of the source where the event was detected.
    element_property: true
    enum_reference_types:
    - Practitioner
    - PractitionerRole
    - Organization
    - CareTeam
    - Patient
    - Device
    - RelatedPerson
    title: The identity of source detecting the event
  resourceType:
    const: AuditEventSource
    default: AuditEventSource
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  site:
    $ref: Reference.yaml
    backref: audit_event_source
    description: Logical source location within the healthcare enterprise network.  For
      example, a hospital or other provider location within a multi-entity provider
      group.
    element_property: true
    enum_reference_types:
    - Location
    title: Logical source location within the enterprise
  type:
    binding_description: Code specifying the type of system that detected and recorded
      the event. Use of these codes is not required but is encouraged to maintain
      translation with DICOM AuditMessage schema.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/security-source-type
    binding_version: null
    description: Code specifying the type of source where event originated.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: The type of source where event originated
    type: array
required:
- observer
title: AuditEventSource
type: object

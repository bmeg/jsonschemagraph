$id: SubscriptionFilterBy
additionalProperties: false
description: Criteria for narrowing the subscription topic stream. The filter properties
  to be applied to narrow the subscription topic stream.  When multiple filters are
  applied, evaluates to true if all the conditions applicable to that resource are
  met; otherwise it returns false (i.e., logical AND). [See https://hl7.org/fhir/R5/SubscriptionFilterBy.html]
  [See https://hl7.org/fhir/R5/SubscriptionFilterBy.html]
links: []
properties:
  _comparator:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``comparator``.
  _filterParameter:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``filterParameter``.
  _modifier:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``modifier``.
  _resourceType:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``resourceType``.
  _value:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``value``.
  comparator:
    binding_description: Search Comparator Codes applied to this filter.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/search-comparator
    binding_version: 5.0.0
    description: Comparator applied to this filter parameter.
    element_property: true
    enum_values:
    - eq
    - ne
    - gt
    - lt
    - ge
    - le
    - sa
    - eb
    - ap
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: eq | ne | gt | lt | ge | le | sa | eb | ap
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  filterParameter:
    description: The filter as defined in the `SubscriptionTopic.canFilterBy.filterParameter`
      element.
    element_property: true
    element_required: true
    pattern: '[ \r\n\t\S]+'
    title: Filter label defined in SubscriptionTopic
    type: string
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifier:
    binding_description: Search Modifier Code applied to this filter.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/search-modifier-code
    binding_version: 5.0.0
    description: Modifier applied to this filter parameter.
    element_property: true
    enum_values:
    - missing
    - exact
    - contains
    - not
    - text
    - in
    - not-in
    - below
    - above
    - type
    - identifier
    - of-type
    - code-text
    - text-advanced
    - iterate
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: missing | exact | contains | not | text | in | not-in | below | above |
      type | identifier | of-type | code-text | text-advanced | iterate
    type: string
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  resourceType:
    binding_description: A type of resource, or a Reference (from all versions)
    binding_strength: extensible
    binding_uri: http://hl7.org/fhir/ValueSet/subscription-types
    binding_version: null
    const: SubscriptionFilterBy
    default: SubscriptionFilterBy
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  value:
    description: The literal value or resource path as is legal in search - for example,
      `Patient/123` or `le1950`.
    element_property: true
    element_required: true
    pattern: '[ \r\n\t\S]+'
    title: Literal value or resource path
    type: string
title: SubscriptionFilterBy
type: object

$id: InsurancePlanPlan
additionalProperties: false
description: Plan details. Details about an insurance plan. [See https://hl7.org/fhir/R5/InsurancePlanPlan.html]
  [See https://hl7.org/fhir/R5/InsurancePlanPlan.html]
links:
- href: Location/{id}
  rel: coverageArea
  targetHints:
    backref:
    - coverageArea_insurance_plan_plan
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Location.yaml
  templatePointers:
    id: /coverageArea/-/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: network
  targetHints:
    backref:
    - network_insurance_plan_plan
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /network/-/reference
  templateRequired:
  - id
properties:
  coverageArea:
    backref: coverageArea_insurance_plan_plan
    description: The geographic region in which a health insurance plan's benefits
      apply.
    element_property: true
    enum_reference_types:
    - Location
    items:
      $ref: Reference.yaml
    title: Where product applies
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  generalCost:
    description: Overall costs associated with the plan.
    element_property: true
    items:
      $ref: InsurancePlanPlanGeneralCost.yaml
    title: Overall costs
    type: array
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  identifier:
    description: Business identifiers assigned to this health insurance plan which
      remain constant as the resource is updated and propagates from server to server.
    element_property: true
    items:
      $ref: Identifier.yaml
    title: Business Identifier for Product
    type: array
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  network:
    backref: network_insurance_plan_plan
    description: Reference to the network that providing the type of coverage.
    element_property: true
    enum_reference_types:
    - Organization
    items:
      $ref: Reference.yaml
    title: What networks provide coverage
    type: array
  resourceType:
    const: InsurancePlanPlan
    default: InsurancePlanPlan
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  specificCost:
    description: Costs associated with the coverage provided by the product.
    element_property: true
    items:
      $ref: InsurancePlanPlanSpecificCost.yaml
    title: Specific costs
    type: array
  type:
    $ref: CodeableConcept.yaml
    description: Type of plan. For example, "Platinum" or "High Deductable".
    element_property: true
    title: Type of plan
title: InsurancePlanPlan
type: object

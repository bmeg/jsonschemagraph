$id: MedicinalProductDefinitionCrossReference
additionalProperties: false
description: Reference to another product, e.g. for linking authorised to investigational
  product. Reference to another product, e.g. for linking authorised to investigational
  product, or a virtual product. [See https://hl7.org/fhir/R5/MedicinalProductDefinitionCrossReference.html]
  [See https://hl7.org/fhir/R5/MedicinalProductDefinitionCrossReference.html]
links:
- href: Reference/{id}
  rel: product_reference
  targetHints:
    backref:
    - codeable_reference
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Reference.yaml
  templatePointers:
    id: /product/reference/reference
  templateRequired:
  - id
properties:
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  product:
    $ref: CodeableReference.yaml
    backref: medicinal_product_definition_cross_reference
    element_property: true
    enum_reference_types:
    - MedicinalProductDefinition
    title: Reference to another product, e.g. for linking authorised to investigational
      product
  resourceType:
    const: MedicinalProductDefinitionCrossReference
    default: MedicinalProductDefinitionCrossReference
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  type:
    $ref: CodeableConcept.yaml
    binding_description: Extra measures defined for a Medicinal Product, such as heightened
      reporting requirements.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/medicinal-product-cross-reference-type
    binding_version: null
    description: The type of relationship, for instance branded to generic, virtual
      to actual product, product to development product (investigational), parallel
      import version.
    element_property: true
    title: The type of relationship, for instance branded to generic or virtual to
      actual product
required:
- product
title: MedicinalProductDefinitionCrossReference
type: object

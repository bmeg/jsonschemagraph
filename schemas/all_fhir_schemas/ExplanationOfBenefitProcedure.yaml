$id: ExplanationOfBenefitProcedure
additionalProperties: false
description: Clinical procedures performed. Procedures performed on the patient relevant
  to the billing items with the claim. [See https://hl7.org/fhir/R5/ExplanationOfBenefitProcedure.html]
  [See https://hl7.org/fhir/R5/ExplanationOfBenefitProcedure.html]
links:
- href: Procedure/{id}
  rel: procedureReference
  targetHints:
    backref:
    - explanation_of_benefit_procedure
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Procedure.yaml
  templatePointers:
    id: /procedureReference/reference
  templateRequired:
  - id
- href: Device/{id}
  rel: udi
  targetHints:
    backref:
    - udi_explanation_of_benefit_procedure
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Device.yaml
  templatePointers:
    id: /udi/-/reference
  templateRequired:
  - id
properties:
  _date:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``date``.
  _sequence:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``sequence``.
  date:
    description: Date and optionally time the procedure was performed.
    element_property: true
    format: date-time
    title: When the procedure was performed
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  procedureCodeableConcept:
    $ref: CodeableConcept.yaml
    description: The code or reference to a Procedure resource which identifies the
      clinical intervention performed.
    element_property: true
    one_of_many: procedure
    one_of_many_required: true
    title: Specific clinical procedure
  procedureReference:
    $ref: Reference.yaml
    backref: explanation_of_benefit_procedure
    description: The code or reference to a Procedure resource which identifies the
      clinical intervention performed.
    element_property: true
    enum_reference_types:
    - Procedure
    one_of_many: procedure
    one_of_many_required: true
    title: Specific clinical procedure
  resourceType:
    const: ExplanationOfBenefitProcedure
    default: ExplanationOfBenefitProcedure
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  sequence:
    description: A number to uniquely identify procedure entries.
    element_property: true
    element_required: true
    exclusiveMinimum: 0
    title: Procedure instance identifier
    type: integer
  type:
    binding_description: Example procedure type codes.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/ex-procedure-type
    binding_version: null
    description: When the condition was observed or the relative ranking.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Category of Procedure
    type: array
  udi:
    backref: udi_explanation_of_benefit_procedure
    description: Unique Device Identifiers associated with this line item.
    element_property: true
    enum_reference_types:
    - Device
    items:
      $ref: Reference.yaml
    title: Unique device identifier
    type: array
title: ExplanationOfBenefitProcedure
type: object

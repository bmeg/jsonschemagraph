$id: MeasureGroupStratifier
additionalProperties: false
description: Stratifier criteria for the measure. The stratifier criteria for the
  measure report, specified as either the name of a valid CQL expression defined within
  a referenced library or a valid FHIR Resource Path. [See https://hl7.org/fhir/R5/MeasureGroupStratifier.html]
  [See https://hl7.org/fhir/R5/MeasureGroupStratifier.html]
links:
- href: Group/{id}
  rel: groupDefinition
  targetHints:
    backref:
    - measure_group_stratifier
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Group.yaml
  templatePointers:
    id: /groupDefinition/reference
  templateRequired:
  - id
- href: Group/{id}
  rel: component_groupDefinition
  targetHints:
    backref:
    - measure_group_stratifier_component
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Group.yaml
  templatePointers:
    id: /component/-/groupDefinition/reference
  templateRequired:
  - id
properties:
  _description:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``description``.
  _linkId:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``linkId``.
  code:
    $ref: CodeableConcept.yaml
    binding_description: Meaning of the stratifier.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/measure-stratifier-example
    binding_version: null
    description: Indicates a meaning for the stratifier. This can be as simple as
      a unique identifier, or it can establish meaning in a broader context by drawing
      from a terminology, allowing stratifiers to be correlated across measures.
    element_property: true
    title: Meaning of the stratifier
  component:
    description: A component of the stratifier criteria for the measure report, specified
      as either the name of a valid CQL expression defined within a referenced library
      or a valid FHIR Resource Path.
    element_property: true
    items:
      $ref: MeasureGroupStratifierComponent.yaml
    title: Stratifier criteria component for the measure
    type: array
  criteria:
    $ref: Expression.yaml
    description: An expression that specifies the criteria for the stratifier. This
      is typically the name of an expression defined within a referenced library,
      but it may also be a path to a stratifier element.
    element_property: true
    title: How the measure should be stratified
  description:
    description: The human readable description of this stratifier criteria.
    element_property: true
    pattern: \s*(\S|\s)*
    title: The human readable description of this stratifier
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  groupDefinition:
    $ref: Reference.yaml
    backref: measure_group_stratifier
    description: A Group resource that defines this population as a set of characteristics.
    element_property: true
    enum_reference_types:
    - Group
    title: A group resource that defines this population
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  linkId:
    description: An identifier that is unique within the Measure allowing linkage
      to the equivalent item in a MeasureReport resource.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for stratifier in measure
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  resourceType:
    const: MeasureGroupStratifier
    default: MeasureGroupStratifier
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
title: MeasureGroupStratifier
type: object

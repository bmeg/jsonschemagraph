$id: AccountGuarantor
additionalProperties: false
description: The parties ultimately responsible for balancing the Account. The parties
  responsible for balancing the account if other payment options fall short. [See
  https://hl7.org/fhir/R5/AccountGuarantor.html] [See https://hl7.org/fhir/R5/AccountGuarantor.html]
links:
- href: Patient/{id}
  rel: party_Patient
  targetHints:
    backref:
    - account_guarantor
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /party/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: party_RelatedPerson
  targetHints:
    backref:
    - account_guarantor
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /party/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: party_Organization
  targetHints:
    backref:
    - account_guarantor
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /party/reference
  templateRequired:
  - id
properties:
  _onHold:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``onHold``.
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  onHold:
    description: A guarantor may be placed on credit hold or otherwise have their
      role temporarily suspended.
    element_property: true
    title: Credit or other hold applied
    type: boolean
  party:
    $ref: Reference.yaml
    backref: account_guarantor
    description: The entity who is responsible.
    element_property: true
    enum_reference_types:
    - Patient
    - RelatedPerson
    - Organization
    title: Responsible entity
  period:
    $ref: Period.yaml
    description: The timeframe during which the guarantor accepts responsibility for
      the account.
    element_property: true
    title: Guarantee account during
  resourceType:
    const: AccountGuarantor
    default: AccountGuarantor
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
required:
- party
title: AccountGuarantor
type: object

$id: MolecularSequenceRelative
additionalProperties: false
description: A sequence defined relative to another sequence. [See https://hl7.org/fhir/R5/MolecularSequenceRelative.html]
  [See https://hl7.org/fhir/R5/MolecularSequenceRelative.html]
links:
- href: MolecularSequence/{id}
  rel: startingSequence_sequenceReference
  targetHints:
    backref:
    - molecular_sequence_relative_starting_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: MolecularSequence.yaml
  templatePointers:
    id: /startingSequence/sequenceReference/reference
  templateRequired:
  - id
properties:
  _ordinalPosition:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``ordinalPosition``.
  coordinateSystem:
    $ref: CodeableConcept.yaml
    binding_description: Genomic coordinate system.
    binding_strength: extensible
    binding_uri: http://loinc.org/LL5323-2/
    binding_version: null
    description: These are different ways of identifying nucleotides or amino acids
      within a sequence. Different databases and file types may use different systems.
      For detail definitions, see https://loinc.org/92822-6/ for more detail.
    element_property: true
    title: Ways of identifying nucleotides or amino acids within a sequence
  edit:
    element_property: true
    items:
      $ref: MolecularSequenceRelativeEdit.yaml
    title: Changes in sequence from the starting sequence
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  ordinalPosition:
    element_property: true
    title: Indicates the order in which the sequence should be considered when putting
      multiple 'relative' elements together
    type: integer
  resourceType:
    const: MolecularSequenceRelative
    default: MolecularSequenceRelative
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  sequenceRange:
    $ref: Range.yaml
    element_property: true
    title: Indicates the nucleotide range in the composed sequence when multiple 'relative'
      elements are used together
  startingSequence:
    $ref: MolecularSequenceRelativeStartingSequence.yaml
    description: A sequence that is used as a starting sequence to describe variants
      that are present in a sequence analyzed.
    element_property: true
    title: A sequence used as starting sequence
required:
- coordinateSystem
title: MolecularSequenceRelative
type: object

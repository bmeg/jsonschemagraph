$id: BundleEntryResponse
additionalProperties: false
description: Results of execution (transaction/batch/history). Indicates the results
  of processing the corresponding 'request' entry in the batch or transaction being
  responded to or what the results of an operation where when returning history. [See
  https://hl7.org/fhir/R5/BundleEntryResponse.html] [See https://hl7.org/fhir/R5/BundleEntryResponse.html]
links: []
properties:
  _etag:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``etag``.
  _lastModified:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``lastModified``.
  _location:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``location``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  etag:
    description: The Etag for the resource, if the operation for the entry produced
      a versioned resource (see [Resource Metadata and Versioning](http.html#versioning)
      and [Managing Resource Contention](http.html#concurrency)).
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: The Etag for the resource (if relevant)
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  lastModified:
    description: The date/time that the resource was modified on the server.
    element_property: true
    format: date-time
    title: Server's date time modified
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  location:
    description: The location header created by processing this operation, populated
      if the operation returns a location.
    element_property: true
    pattern: \S*
    title: The location (if the operation returns a location)
    type: string
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  outcome:
    $ref: Resource.yaml
    description: An OperationOutcome containing hints and warnings produced as part
      of processing this entry in a batch or transaction.
    element_property: true
    title: OperationOutcome with hints and warnings (for batch/transaction)
  resourceType:
    const: BundleEntryResponse
    default: BundleEntryResponse
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    description: The status code returned by processing this entry. The status SHALL
      start with a 3 digit HTTP code (e.g. 404) and may contain the standard HTTP
      description associated with the status code.
    element_property: true
    element_required: true
    pattern: '[ \r\n\t\S]+'
    title: Status response code (text optional)
    type: string
title: BundleEntryResponse
type: object

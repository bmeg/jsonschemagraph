$id: MolecularSequence
additionalProperties: false
description: Representation of a molecular sequence. [See https://hl7.org/fhir/R5/MolecularSequence.html]
links:
- href: Device/{id}
  rel: device
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Device.yaml
  templatePointers:
    id: /device/reference
  templateRequired:
  - id
- href: Resource/{id}
  rel: focus
  targetHints:
    backref:
    - focus_molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Resource.yaml
  templatePointers:
    id: /focus/-/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: performer
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /performer/reference
  templateRequired:
  - id
- href: Specimen/{id}
  rel: specimen
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Specimen.yaml
  templatePointers:
    id: /specimen/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: subject_Patient
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
- href: Group/{id}
  rel: subject_Group
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Group.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
- href: Substance/{id}
  rel: subject_Substance
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Substance.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
- href: BiologicallyDerivedProduct/{id}
  rel: subject_BiologicallyDerivedProduct
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: BiologicallyDerivedProduct.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
- href: NutritionProduct/{id}
  rel: subject_NutritionProduct
  targetHints:
    backref:
    - molecular_sequence
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: NutritionProduct.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
properties:
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _literal:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``literal``.
  _type:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``type``.
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  device:
    $ref: Reference.yaml
    backref: molecular_sequence
    description: The method for sequencing, for example, chip information.
    element_property: true
    enum_reference_types:
    - Device
    title: The method for sequencing
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  focus:
    backref: focus_molecular_sequence
    description: The actual focus of a molecular sequence when it is not the patient
      of record representing something or someone associated with the patient such
      as a spouse, parent, child, or sibling. For example, in trio testing, the subject
      would be the child (proband) and the focus would be the parent.
    element_property: true
    enum_reference_types:
    - Resource
    items:
      $ref: Reference.yaml
    title: What the molecular sequence is about, when it is not about the subject
      of record
    type: array
  formatted:
    description: Sequence that was observed as file content. Can be an actual file
      contents, or referenced by a URL to an external system.
    element_property: true
    items:
      $ref: Attachment.yaml
    title: Embedded file or a link (URL) which contains content to represent the sequence
    type: array
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  identifier:
    description: A unique identifier for this particular sequence instance.
    element_property: true
    items:
      $ref: Identifier.yaml
    title: Unique ID for this particular sequence
    type: array
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  literal:
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Sequence that was observed
    type: string
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  performer:
    $ref: Reference.yaml
    backref: molecular_sequence
    description: The organization or lab that should be responsible for this result.
    element_property: true
    enum_reference_types:
    - Organization
    title: Who should be responsible for test result
  relative:
    element_property: true
    items:
      $ref: MolecularSequenceRelative.yaml
    title: A sequence defined relative to another sequence
    type: array
  resourceType:
    const: MolecularSequence
    default: MolecularSequence
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  specimen:
    $ref: Reference.yaml
    backref: molecular_sequence
    element_property: true
    enum_reference_types:
    - Specimen
    title: Specimen used for sequencing
  subject:
    $ref: Reference.yaml
    backref: molecular_sequence
    description: Indicates the subject this sequence is associated too.
    element_property: true
    enum_reference_types:
    - Patient
    - Group
    - Substance
    - BiologicallyDerivedProduct
    - NutritionProduct
    title: Subject this sequence is associated too
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
  type:
    binding_description: Type if a sequence -- DNA, RNA, or amino acid sequence.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/sequence-type
    binding_version: 5.0.0
    description: Amino Acid Sequence/ DNA Sequence / RNA Sequence.
    element_property: true
    enum_values:
    - aa
    - dna
    - rna
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: aa | dna | rna
    type: string
title: MolecularSequence
type: object

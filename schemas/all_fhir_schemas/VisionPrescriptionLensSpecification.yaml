$id: VisionPrescriptionLensSpecification
additionalProperties: false
description: Vision lens authorization. Contain the details of  the individual lens
  specifications and serves as the authorization for the fullfillment by certified
  professionals. [See https://hl7.org/fhir/R5/VisionPrescriptionLensSpecification.html]
  [See https://hl7.org/fhir/R5/VisionPrescriptionLensSpecification.html]
links:
- href: Practitioner/{id}
  rel: note_authorReference_Practitioner
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: note_authorReference_PractitionerRole
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: note_authorReference_Patient
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: note_authorReference_RelatedPerson
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: note_authorReference_Organization
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
properties:
  _add:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``add``.
  _axis:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``axis``.
  _backCurve:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``backCurve``.
  _brand:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``brand``.
  _color:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``color``.
  _cylinder:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``cylinder``.
  _diameter:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``diameter``.
  _eye:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``eye``.
  _power:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``power``.
  _sphere:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``sphere``.
  add:
    description: Power adjustment for multifocal lenses measured in dioptres (0.25
      units).
    element_property: true
    title: Added power for multifocal levels
    type: number
  axis:
    description: Adjustment for astigmatism measured in integer degrees.
    element_property: true
    title: Lens meridian which contain no power for astigmatism
    type: integer
  backCurve:
    description: Back curvature measured in millimetres.
    element_property: true
    title: Contact lens back curvature
    type: number
  brand:
    description: Brand recommendations or restrictions.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Brand required
    type: string
  color:
    description: Special color or pattern.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Color required
    type: string
  cylinder:
    description: Power adjustment for astigmatism measured in dioptres (0.25 units).
    element_property: true
    title: Lens power for astigmatism
    type: number
  diameter:
    description: Contact lens diameter measured in millimetres.
    element_property: true
    title: Contact lens diameter
    type: number
  duration:
    $ref: Quantity.yaml
    description: The recommended maximum wear period for the lens.
    element_property: true
    title: Lens wear duration
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  eye:
    binding_description: A coded concept listing the eye codes.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/vision-eye-codes
    binding_version: 5.0.0
    description: The eye for which the lens specification applies.
    element_property: true
    element_required: true
    enum_values:
    - right
    - left
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: right | left
    type: string
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  note:
    description: Notes for special requirements such as coatings and lens materials.
    element_property: true
    items:
      $ref: Annotation.yaml
    title: Notes for coatings
    type: array
  power:
    description: Contact lens power measured in dioptres (0.25 units).
    element_property: true
    title: Contact lens power
    type: number
  prism:
    description: Allows for adjustment on two axis.
    element_property: true
    items:
      $ref: VisionPrescriptionLensSpecificationPrism.yaml
    title: Eye alignment compensation
    type: array
  product:
    $ref: CodeableConcept.yaml
    binding_description: A coded concept describing the vision products.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/vision-product
    binding_version: null
    description: Identifies the type of vision correction product which is required
      for the patient.
    element_property: true
    title: Product to be supplied
  resourceType:
    const: VisionPrescriptionLensSpecification
    default: VisionPrescriptionLensSpecification
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  sphere:
    description: Lens power measured in dioptres (0.25 units).
    element_property: true
    title: Power of the lens
    type: number
required:
- product
title: VisionPrescriptionLensSpecification
type: object

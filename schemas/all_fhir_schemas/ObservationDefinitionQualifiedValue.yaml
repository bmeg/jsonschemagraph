$id: ObservationDefinitionQualifiedValue
additionalProperties: false
description: Set of qualified values for observation results. A set of qualified values
  associated with a context and a set of conditions -  provides a range for quantitative
  and ordinal observations and a collection of value sets for qualitative observations.
  [See https://hl7.org/fhir/R5/ObservationDefinitionQualifiedValue.html] [See https://hl7.org/fhir/R5/ObservationDefinitionQualifiedValue.html]
  [See https://hl7.org/fhir/R5/ObservationDefinitionQualifiedValue.html]
links: []
properties:
  _abnormalCodedValueSet:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``abnormalCodedValueSet``.
  _condition:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``condition``.
  _criticalCodedValueSet:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``criticalCodedValueSet``.
  _gender:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``gender``.
  _normalCodedValueSet:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``normalCodedValueSet``.
  _rangeCategory:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``rangeCategory``.
  _validCodedValueSet:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``validCodedValueSet``.
  abnormalCodedValueSet:
    description: The set of abnormal coded results for qualitative observations  that
      match the criteria of this set of qualified values.
    element_property: true
    enum_reference_types:
    - ValueSet
    pattern: \S*
    title: Value set of abnormal coded values as part of this set of qualified values
    type: string
  age:
    $ref: Range.yaml
    description: The age range this  set of qualified values applies to.
    element_property: true
    title: Applicable age range for the set of qualified values
  appliesTo:
    binding_description: null
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/referencerange-appliesto
    binding_version: null
    description: The target population this  set of qualified values applies to.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Targetted population for the set of qualified values
    type: array
  condition:
    description: Text based condition for which the the set of qualified values is
      valid.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Condition associated with the set of qualified values
    type: string
  context:
    $ref: CodeableConcept.yaml
    binding_description: null
    binding_strength: extensible
    binding_uri: http://hl7.org/fhir/ValueSet/referencerange-meaning
    binding_version: null
    description: A concept defining the context for this set of qualified values.
    element_property: true
    title: Context qualifier for the set of qualified values
  criticalCodedValueSet:
    description: The set of critical coded results for qualitative observations  that
      match the criteria of this set of qualified values.
    element_property: true
    enum_reference_types:
    - ValueSet
    pattern: \S*
    title: Value set of critical coded values as part of this set of qualified values
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  gender:
    binding_description: null
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/administrative-gender
    binding_version: 5.0.0
    description: The gender this  set of qualified values applies to.
    element_property: true
    enum_values:
    - male
    - female
    - other
    - unknown
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: male | female | other | unknown
    type: string
  gestationalAge:
    $ref: Range.yaml
    description: The gestational age this  set of qualified values applies to.
    element_property: true
    title: Applicable gestational age range for the set of qualified values
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  normalCodedValueSet:
    description: The set of normal coded results for qualitative observations  that
      match the criteria of this set of qualified values.
    element_property: true
    enum_reference_types:
    - ValueSet
    pattern: \S*
    title: Value set of normal coded values as part of this set of qualified values
    type: string
  range:
    $ref: Range.yaml
    description: The range of values defined for continuous or ordinal observations
      that match the criteria of this set of qualified values.
    element_property: true
    title: The range for continuous or ordinal observations
  rangeCategory:
    binding_description: null
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/observation-range-category
    binding_version: 5.0.0
    description: The category of range of values for continuous or ordinal observations
      that match the criteria of this set of qualified values.
    element_property: true
    enum_values:
    - reference
    - critical
    - absolute
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: reference | critical | absolute
    type: string
  resourceType:
    const: ObservationDefinitionQualifiedValue
    default: ObservationDefinitionQualifiedValue
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  validCodedValueSet:
    description: The set of valid coded results for qualitative observations  that
      match the criteria of this set of qualified values.
    element_property: true
    enum_reference_types:
    - ValueSet
    pattern: \S*
    title: Value set of valid coded values as part of this set of qualified values
    type: string
title: ObservationDefinitionQualifiedValue
type: object

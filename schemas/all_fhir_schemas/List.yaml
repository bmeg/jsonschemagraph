$id: List
additionalProperties: false
description: A list is a curated collection of resources. A List is a curated collection
  of resources, for things such as problem lists, allergy lists, facility list, organization
  list, etc. [See https://hl7.org/fhir/R5/List.html]
links:
- href: Encounter/{id}
  rel: encounter
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Encounter.yaml
  templatePointers:
    id: /encounter/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: source_Practitioner
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: source_PractitionerRole
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: source_Patient
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: Device/{id}
  rel: source_Device
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Device.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: source_Organization
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: source_RelatedPerson
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: CareTeam/{id}
  rel: source_CareTeam
  targetHints:
    backref:
    - list
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: CareTeam.yaml
  templatePointers:
    id: /source/reference
  templateRequired:
  - id
- href: Resource/{id}
  rel: subject
  targetHints:
    backref:
    - subject_list
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Resource.yaml
  templatePointers:
    id: /subject/-/reference
  templateRequired:
  - id
- href: Resource/{id}
  rel: entry_item
  targetHints:
    backref:
    - list_entry
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Resource.yaml
  templatePointers:
    id: /entry/-/item/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: note_authorReference_Practitioner
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: note_authorReference_PractitionerRole
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: note_authorReference_Patient
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: note_authorReference_RelatedPerson
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: note_authorReference_Organization
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
properties:
  _date:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``date``.
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _mode:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``mode``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  _title:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``title``.
  code:
    $ref: CodeableConcept.yaml
    binding_description: What the purpose of a list is.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/list-example-codes
    binding_version: null
    description: This code defines the purpose of the list - why it was created.
    element_property: true
    title: What the purpose of this list is
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  date:
    description: Date list was last reviewed/revised and determined to be 'current'.
    element_property: true
    format: date-time
    title: When the list was prepared
    type: string
  emptyReason:
    $ref: CodeableConcept.yaml
    binding_description: If a list is empty, why it is empty.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/list-empty-reason
    binding_version: null
    description: If the list is empty, why the list is empty.
    element_property: true
    title: Why list is empty
  encounter:
    $ref: Reference.yaml
    backref: list
    description: The encounter that is the context in which this list was created.
    element_property: true
    enum_reference_types:
    - Encounter
    title: Context in which list created
  entry:
    description: Entries in this list.
    element_property: true
    items:
      $ref: ListEntry.yaml
    title: Entries in the list
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  identifier:
    description: Identifier for the List assigned for business purposes outside the
      context of FHIR.
    element_property: true
    items:
      $ref: Identifier.yaml
    title: Business identifier
    type: array
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  mode:
    binding_description: The processing mode that applies to this list.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/list-mode
    binding_version: 5.0.0
    description: How this list was prepared - whether it is a working list that is
      suitable for being maintained on an ongoing basis, or if it represents a snapshot
      of a list of items from another source, or whether it is a prepared list where
      items may be marked as added, modified or deleted.
    element_property: true
    element_required: true
    enum_values:
    - working
    - snapshot
    - changes
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: working | snapshot | changes
    type: string
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  note:
    description: Comments that apply to the overall list.
    element_property: true
    items:
      $ref: Annotation.yaml
    title: Comments about the list
    type: array
  orderedBy:
    $ref: CodeableConcept.yaml
    binding_description: What order applies to the items in a list.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/list-order
    binding_version: null
    description: What order applies to the items in the list.
    element_property: true
    title: What order the list has
  resourceType:
    const: List
    default: List
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  source:
    $ref: Reference.yaml
    backref: list
    description: The entity responsible for deciding what the contents of the list
      were. Where the list was created by a human, this is the same as the author
      of the list.
    element_property: true
    enum_reference_types:
    - Practitioner
    - PractitionerRole
    - Patient
    - Device
    - Organization
    - RelatedPerson
    - CareTeam
    title: Who and/or what defined the list contents (aka Author)
  status:
    binding_description: The current state of the list.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/list-status
    binding_version: 5.0.0
    description: Indicates the current state of this list.
    element_property: true
    element_required: true
    enum_values:
    - current
    - retired
    - entered-in-error
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: current | retired | entered-in-error
    type: string
  subject:
    backref: subject_list
    description: The common subject(s) (or patient(s)) of the resources that are in
      the list if there is one (or a set of subjects).
    element_property: true
    enum_reference_types:
    - Resource
    items:
      $ref: Reference.yaml
    title: If all resources have the same subject(s)
    type: array
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
  title:
    description: A label for the list assigned by the author.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Descriptive name for the list
    type: string
title: List
type: object

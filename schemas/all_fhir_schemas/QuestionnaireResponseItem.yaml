$id: QuestionnaireResponseItem
additionalProperties: false
description: Groups and questions. A group or question item from the original questionnaire
  for which answers are provided. [See https://hl7.org/fhir/R5/QuestionnaireResponseItem.html]
  [See https://hl7.org/fhir/R5/QuestionnaireResponseItem.html] [See https://hl7.org/fhir/R5/QuestionnaireResponseItem.html]
links:
- href: Resource/{id}
  rel: answer_valueReference
  targetHints:
    backref:
    - questionnaire_response_item_answer
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Resource.yaml
  templatePointers:
    id: /answer/-/valueReference/reference
  templateRequired:
  - id
properties:
  _definition:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``definition``.
  _linkId:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``linkId``.
  _text:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``text``.
  answer:
    description: The respondent's answer(s) to the question.
    element_property: true
    items:
      $ref: QuestionnaireResponseItemAnswer.yaml
    title: The response(s) to the question
    type: array
  definition:
    description: A reference to an [ElementDefinition](elementdefinition.html) that
      provides the details for the item.
    element_property: true
    pattern: \S*
    title: ElementDefinition - details for the item
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  item:
    description: Sub-questions, sub-groups or display items nested beneath a group.
    element_property: true
    items:
      $ref: QuestionnaireResponseItem.yaml
    title: Child items of group item
    type: array
  linkId:
    description: The item from the Questionnaire that corresponds to this item in
      the QuestionnaireResponse resource.
    element_property: true
    element_required: true
    pattern: '[ \r\n\t\S]+'
    title: Pointer to specific item from Questionnaire
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  resourceType:
    const: QuestionnaireResponseItem
    default: QuestionnaireResponseItem
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  text:
    description: Text that is displayed above the contents of the group or as the
      text of the question being answered.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Name for group or question text
    type: string
title: QuestionnaireResponseItem
type: object

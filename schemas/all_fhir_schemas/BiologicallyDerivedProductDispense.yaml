$id: BiologicallyDerivedProductDispense
additionalProperties: false
description: A record of dispensation of a biologically derived product. This resource
  reflects an instance of a biologically derived product dispense. The supply or dispense
  of a biologically derived product from the supply organization or department (e.g.
  hospital transfusion laboratory) to the clinical team responsible for clinical application.
  [See https://hl7.org/fhir/R5/BiologicallyDerivedProductDispense.html]
links:
- href: ServiceRequest/{id}
  rel: basedOn
  targetHints:
    backref:
    - basedOn_biologically_derived_product_dispense
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: ServiceRequest.yaml
  templatePointers:
    id: /basedOn/-/reference
  templateRequired:
  - id
- href: Location/{id}
  rel: destination
  targetHints:
    backref:
    - destination_biologically_derived_product_dispense
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Location.yaml
  templatePointers:
    id: /destination/reference
  templateRequired:
  - id
- href: Location/{id}
  rel: location
  targetHints:
    backref:
    - location_biologically_derived_product_dispense
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Location.yaml
  templatePointers:
    id: /location/reference
  templateRequired:
  - id
- href: BiologicallyDerivedProductDispense/{id}
  rel: partOf
  targetHints:
    backref:
    - partOf_biologically_derived_product_dispense
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: BiologicallyDerivedProductDispense.yaml
  templatePointers:
    id: /partOf/-/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: patient
  targetHints:
    backref:
    - biologically_derived_product_dispense
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /patient/reference
  templateRequired:
  - id
- href: BiologicallyDerivedProduct/{id}
  rel: product
  targetHints:
    backref:
    - biologically_derived_product_dispense
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: BiologicallyDerivedProduct.yaml
  templatePointers:
    id: /product/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: note_authorReference_Practitioner
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: note_authorReference_PractitionerRole
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: note_authorReference_Patient
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: note_authorReference_RelatedPerson
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: note_authorReference_Organization
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: performer_actor
  targetHints:
    backref:
    - biologically_derived_product_dispense_performer
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /performer/-/actor/reference
  templateRequired:
  - id
properties:
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _preparedDate:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``preparedDate``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  _usageInstruction:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``usageInstruction``.
  _whenHandedOver:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``whenHandedOver``.
  basedOn:
    backref: basedOn_biologically_derived_product_dispense
    description: The order or request that the dispense is fulfilling. This is a reference
      to a ServiceRequest resource.
    element_property: true
    enum_reference_types:
    - ServiceRequest
    items:
      $ref: Reference.yaml
    title: The order or request that this dispense is fulfilling
    type: array
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  destination:
    $ref: Reference.yaml
    backref: destination_biologically_derived_product_dispense
    description: Link to a resource identifying the physical location that the product
      was dispatched to.
    element_property: true
    enum_reference_types:
    - Location
    title: Where the product was dispatched to
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  identifier:
    description: 'Unique instance identifiers assigned to a biologically derived product
      dispense. Note: This is a business identifier, not a resource identifier.'
    element_property: true
    items:
      $ref: Identifier.yaml
    title: Business identifier for this dispense
    type: array
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  location:
    $ref: Reference.yaml
    backref: location_biologically_derived_product_dispense
    description: The physical location where the dispense was performed.
    element_property: true
    enum_reference_types:
    - Location
    title: Where the dispense occurred
  matchStatus:
    $ref: CodeableConcept.yaml
    binding_description: Describes the type of matching between the recipient and
      origin of the dispensed product.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-match-status
    binding_version: null
    element_property: true
    title: Indicates the type of matching associated with the dispense
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  note:
    element_property: true
    items:
      $ref: Annotation.yaml
    title: Additional notes
    type: array
  originRelationshipType:
    $ref: CodeableConcept.yaml
    binding_description: Describes the relationship between the recipient and origin
      of the dispensed product.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-origin-relationship
    binding_version: null
    description: Indicates the relationship between the donor of the biologically
      derived product and the intended recipient.
    element_property: true
    title: Relationship between the donor and intended recipient
  partOf:
    backref: partOf_biologically_derived_product_dispense
    description: A larger event of which this particular event is a component.
    element_property: true
    enum_reference_types:
    - BiologicallyDerivedProductDispense
    items:
      $ref: Reference.yaml
    title: Short description
    type: array
  patient:
    $ref: Reference.yaml
    backref: biologically_derived_product_dispense
    description: A link to a resource representing the patient that the product is
      dispensed for.
    element_property: true
    enum_reference_types:
    - Patient
    title: The intended recipient of the dispensed product
  performer:
    element_property: true
    items:
      $ref: BiologicallyDerivedProductDispensePerformer.yaml
    title: Indicates who or what performed an action
    type: array
  preparedDate:
    description: When the product was selected/ matched.
    element_property: true
    format: date-time
    title: When product was selected/matched
    type: string
  product:
    $ref: Reference.yaml
    backref: biologically_derived_product_dispense
    description: A link to a resource identifying the biologically derived product
      that is being dispensed.
    element_property: true
    enum_reference_types:
    - BiologicallyDerivedProduct
    title: The BiologicallyDerivedProduct that is dispensed
  quantity:
    $ref: Quantity.yaml
    description: 'The amount of product in the dispense. Quantity will depend on the
      product being dispensed. Examples are: volume; cell count; concentration.'
    element_property: true
    title: Amount dispensed
  resourceType:
    const: BiologicallyDerivedProductDispense
    default: BiologicallyDerivedProductDispense
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    binding_description: Describes the lifecycle of the dispense.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/biologicallyderivedproductdispense-status
    binding_version: 5.0.0
    description: A code specifying the state of the dispense event.
    element_property: true
    element_required: true
    enum_values:
    - preparation
    - in-progress
    - allocated
    - issued
    - unfulfilled
    - returned
    - entered-in-error
    - unknown
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: preparation | in-progress | allocated | issued | unfulfilled | returned
      | entered-in-error | unknown
    type: string
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
  usageInstruction:
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Specific instructions for use
    type: string
  whenHandedOver:
    description: When the product was dispatched for clinical use.
    element_property: true
    format: date-time
    title: When the product was dispatched
    type: string
required:
- patient
- product
title: BiologicallyDerivedProductDispense
type: object

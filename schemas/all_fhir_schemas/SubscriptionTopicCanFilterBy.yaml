$id: SubscriptionTopicCanFilterBy
additionalProperties: false
description: Properties by which a Subscription can filter notifications from the
  SubscriptionTopic. List of properties by which Subscriptions on the SubscriptionTopic
  can be filtered. May be defined Search Parameters (e.g., Encounter.patient) or parameters
  defined within this SubscriptionTopic context (e.g., hub.event). [See https://hl7.org/fhir/R5/SubscriptionTopicCanFilterBy.html]
  [See https://hl7.org/fhir/R5/SubscriptionTopicCanFilterBy.html]
links: []
properties:
  _comparator:
    items:
      $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``comparator``.
    type: array
  _description:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``description``.
  _filterDefinition:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``filterDefinition``.
  _filterParameter:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``filterParameter``.
  _modifier:
    items:
      $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``modifier``.
    type: array
  _resource:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``resource``.
  comparator:
    binding_description: Search Comparator Codes supported in this filter.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/search-comparator
    binding_version: 5.0.0
    description: Comparators allowed for the filter parameter.
    element_property: true
    enum_values:
    - eq
    - ne
    - gt
    - lt
    - ge
    - le
    - sa
    - eb
    - ap
    items:
      pattern: ^[^\s]+(\s[^\s]+)*$
      type: string
    title: eq | ne | gt | lt | ge | le | sa | eb | ap
    type: array
  description:
    description: Description of how this filtering parameter is intended to be used.
    element_property: true
    pattern: \s*(\S|\s)*
    title: Description of this filter parameter
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  filterDefinition:
    description: Either the canonical URL to a search parameter (like "http://hl7.org/fhir/SearchParameter/encounter-patient")
      or the officially-defined URI for a shared filter concept (like "http://example.org/concepts/shared-common-event").
    element_property: true
    pattern: \S*
    title: Canonical URL for a filterParameter definition
    type: string
  filterParameter:
    description: Either the canonical URL to a search parameter (like "http://hl7.org/fhir/SearchParameter/encounter-patient")
      or topic-defined parameter (like "hub.event") which is a label for the filter.
    element_property: true
    element_required: true
    pattern: '[ \r\n\t\S]+'
    title: Human-readable and computation-friendly name for a filter parameter usable
      by subscriptions on this topic, via Subscription.filterBy.filterParameter
    type: string
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifier:
    binding_description: Search Modifier Codes supported in this filter.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/search-modifier-code
    binding_version: 5.0.0
    description: Modifiers allowed for the filter parameter.
    element_property: true
    enum_values:
    - missing
    - exact
    - contains
    - not
    - text
    - in
    - not-in
    - below
    - above
    - type
    - identifier
    - of-type
    - code-text
    - text-advanced
    - iterate
    items:
      pattern: ^[^\s]+(\s[^\s]+)*$
      type: string
    title: missing | exact | contains | not | text | in | not-in | below | above |
      type | identifier | of-type | code-text | text-advanced | iterate
    type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  resource:
    binding_description: A type of resource, or a Reference (from all versions)
    binding_strength: extensible
    binding_uri: http://hl7.org/fhir/ValueSet/subscription-types
    binding_version: null
    description: URL of the Resource that is the type used in this filter. This is
      the "focus" of the topic (or one of them if there are more than one). It will
      be the same, a generality, or a specificity of SubscriptionTopic.resourceTrigger.resource
      or SubscriptionTopic.eventTrigger.resource when they are present.
    element_property: true
    pattern: \S*
    title: URL of the triggering Resource that this filter applies to
    type: string
  resourceType:
    const: SubscriptionTopicCanFilterBy
    default: SubscriptionTopicCanFilterBy
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
title: SubscriptionTopicCanFilterBy
type: object

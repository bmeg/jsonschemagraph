$id: VerificationResult
additionalProperties: false
description: Describes validation requirements, source(s), status and dates for one
  or more elements. [See https://hl7.org/fhir/R5/VerificationResult.html]
links:
- href: Resource/{id}
  rel: target
  targetHints:
    backref:
    - target_verification_result
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Resource.yaml
  templatePointers:
    id: /target/-/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: attestation_onBehalfOf_Organization
  targetHints:
    backref:
    - onBehalfOf_verification_result_attestation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /attestation/onBehalfOf/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: attestation_onBehalfOf_Practitioner
  targetHints:
    backref:
    - onBehalfOf_verification_result_attestation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /attestation/onBehalfOf/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: attestation_onBehalfOf_PractitionerRole
  targetHints:
    backref:
    - onBehalfOf_verification_result_attestation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /attestation/onBehalfOf/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: attestation_who_Practitioner
  targetHints:
    backref:
    - who_verification_result_attestation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /attestation/who/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: attestation_who_PractitionerRole
  targetHints:
    backref:
    - who_verification_result_attestation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /attestation/who/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: attestation_who_Organization
  targetHints:
    backref:
    - who_verification_result_attestation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /attestation/who/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: primarySource_who_Organization
  targetHints:
    backref:
    - verification_result_primary_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /primarySource/-/who/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: primarySource_who_Practitioner
  targetHints:
    backref:
    - verification_result_primary_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /primarySource/-/who/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: primarySource_who_PractitionerRole
  targetHints:
    backref:
    - verification_result_primary_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /primarySource/-/who/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: validator_organization
  targetHints:
    backref:
    - verification_result_validator
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /validator/-/organization/reference
  templateRequired:
  - id
properties:
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _lastPerformed:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``lastPerformed``.
  _nextScheduled:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``nextScheduled``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  _statusDate:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``statusDate``.
  _targetLocation:
    items:
      $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``targetLocation``.
    type: array
  attestation:
    $ref: VerificationResultAttestation.yaml
    element_property: true
    title: Information about the entity attesting to information
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  failureAction:
    $ref: CodeableConcept.yaml
    binding_description: The result if validation fails.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-failure-action
    binding_version: null
    description: The result if validation fails (fatal; warning; record only; none).
    element_property: true
    title: fatal | warn | rec-only | none
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  frequency:
    $ref: Timing.yaml
    element_property: true
    title: Frequency of revalidation
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  lastPerformed:
    element_property: true
    format: date-time
    title: The date/time validation was last completed (including failed validations)
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  need:
    $ref: CodeableConcept.yaml
    binding_description: The frequency with which the target must be validated.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-need
    binding_version: null
    description: The frequency with which the target must be validated (none; initial;
      periodic).
    element_property: true
    title: none | initial | periodic
  nextScheduled:
    element_property: true
    format: date
    title: The date when target is next validated, if appropriate
    type: string
  primarySource:
    element_property: true
    items:
      $ref: VerificationResultPrimarySource.yaml
    title: Information about the primary source(s) involved in validation
    type: array
  resourceType:
    const: VerificationResult
    default: VerificationResult
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    binding_description: The validation status of the target.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-status
    binding_version: 5.0.0
    description: The validation status of the target (attested; validated; in process;
      requires revalidation; validation failed; revalidation failed).
    element_property: true
    element_required: true
    enum_values:
    - attested
    - validated
    - in-process
    - req-revalid
    - val-fail
    - reval-fail
    - entered-in-error
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: attested | validated | in-process | req-revalid | val-fail | reval-fail
      | entered-in-error
    type: string
  statusDate:
    element_property: true
    format: date-time
    title: When the validation status was updated
    type: string
  target:
    backref: target_verification_result
    element_property: true
    enum_reference_types:
    - Resource
    items:
      $ref: Reference.yaml
    title: A resource that was validated
    type: array
  targetLocation:
    element_property: true
    items:
      pattern: '[ \r\n\t\S]+'
      type: string
    title: The fhirpath location(s) within the resource that was validated
    type: array
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
  validationProcess:
    binding_description: The primary process by which the target is validated.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-validation-process
    binding_version: null
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: The primary process by which the target is validated (edit check; value
      set; primary source; multiple sources; standalone; in context)
    type: array
  validationType:
    $ref: CodeableConcept.yaml
    binding_description: What the target is validated against.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-validation-type
    binding_version: null
    description: What the target is validated against (nothing; primary source; multiple
      sources).
    element_property: true
    title: nothing | primary | multiple
  validator:
    element_property: true
    items:
      $ref: VerificationResultValidator.yaml
    title: Information about the entity validating information
    type: array
title: VerificationResult
type: object

$id: CareTeam
additionalProperties: false
description: Planned participants in the coordination and delivery of care. The Care
  Team includes all the people and organizations who plan to participate in the coordination
  and delivery of care. [See https://hl7.org/fhir/R5/CareTeam.html]
links:
- href: Organization/{id}
  rel: managingOrganization
  targetHints:
    backref:
    - managingOrganization_care_team
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /managingOrganization/-/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: subject_Patient
  targetHints:
    backref:
    - care_team
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
- href: Group/{id}
  rel: subject_Group
  targetHints:
    backref:
    - care_team
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Group.yaml
  templatePointers:
    id: /subject/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: note_authorReference_Practitioner
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: note_authorReference_PractitionerRole
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: note_authorReference_Patient
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: note_authorReference_RelatedPerson
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: note_authorReference_Organization
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: participant_member_Practitioner
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /participant/-/member/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: participant_member_PractitionerRole
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /participant/-/member/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: participant_member_RelatedPerson
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /participant/-/member/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: participant_member_Patient
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /participant/-/member/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: participant_member_Organization
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /participant/-/member/reference
  templateRequired:
  - id
- href: CareTeam/{id}
  rel: participant_member_CareTeam
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: CareTeam.yaml
  templatePointers:
    id: /participant/-/member/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: participant_onBehalfOf
  targetHints:
    backref:
    - onBehalfOf_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /participant/-/onBehalfOf/reference
  templateRequired:
  - id
- href: Reference/{id}
  rel: reason_reference
  targetHints:
    backref:
    - codeable_reference
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Reference.yaml
  templatePointers:
    id: /reason/-/reference/reference
  templateRequired:
  - id
properties:
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _name:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``name``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  category:
    binding_description: Indicates the type of care team.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/care-team-category
    binding_version: null
    description: Identifies what kind of team.  This is to support differentiation
      between multiple co-existing teams, such as care plan team, episode of care
      team, longitudinal care team.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Type of team
    type: array
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  identifier:
    description: Business identifiers assigned to this care team by the performer
      or other systems which remain constant as the resource is updated and propagates
      from server to server.
    element_property: true
    items:
      $ref: Identifier.yaml
    title: External Ids for this team
    type: array
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  managingOrganization:
    backref: managingOrganization_care_team
    description: The organization responsible for the care team.
    element_property: true
    enum_reference_types:
    - Organization
    items:
      $ref: Reference.yaml
    title: Organization responsible for the care team
    type: array
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  name:
    description: A label for human use intended to distinguish like teams.  E.g. the
      "red" vs. "green" trauma teams.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Name of the team, such as crisis assessment team
    type: string
  note:
    element_property: true
    items:
      $ref: Annotation.yaml
    title: Comments made about the CareTeam
    type: array
  participant:
    description: Identifies all people and organizations who are expected to be involved
      in the care team.
    element_property: true
    items:
      $ref: CareTeamParticipant.yaml
    title: Members of the team
    type: array
  period:
    $ref: Period.yaml
    description: Indicates when the team did (or is intended to) come into effect
      and end.
    element_property: true
    title: Time period team covers
  reason:
    backref: reason_care_team
    binding_description: Indicates the reason for the care team.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/clinical-findings
    binding_version: null
    description: Describes why the care team exists.
    element_property: true
    enum_reference_types:
    - Condition
    items:
      $ref: CodeableReference.yaml
    title: Why the care team exists
    type: array
  resourceType:
    const: CareTeam
    default: CareTeam
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    binding_description: Indicates the status of the care team.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/care-team-status
    binding_version: 5.0.0
    description: Indicates the current state of the care team.
    element_property: true
    enum_values:
    - proposed
    - active
    - suspended
    - inactive
    - entered-in-error
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: proposed | active | suspended | inactive | entered-in-error
    type: string
  subject:
    $ref: Reference.yaml
    backref: care_team
    description: Identifies the patient or group whose intended care is handled by
      the team.
    element_property: true
    enum_reference_types:
    - Patient
    - Group
    title: Who care team is for
  telecom:
    description: A central contact detail for the care team (that applies to all members).
    element_property: true
    items:
      $ref: ContactPoint.yaml
    title: A contact detail for the care team (that applies to all members)
    type: array
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
title: CareTeam
type: object

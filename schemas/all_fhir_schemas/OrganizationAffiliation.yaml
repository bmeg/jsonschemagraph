$id: OrganizationAffiliation
additionalProperties: false
description: Defines an affiliation/association/relationship between 2 distinct organizations,
  that is not a part-of relationship/sub-division relationship. [See https://hl7.org/fhir/R5/OrganizationAffiliation.html]
links:
- href: Endpoint/{id}
  rel: endpoint
  targetHints:
    backref:
    - endpoint_organization_affiliation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Endpoint.yaml
  templatePointers:
    id: /endpoint/-/reference
  templateRequired:
  - id
- href: HealthcareService/{id}
  rel: healthcareService
  targetHints:
    backref:
    - healthcareService_organization_affiliation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: HealthcareService.yaml
  templatePointers:
    id: /healthcareService/-/reference
  templateRequired:
  - id
- href: Location/{id}
  rel: location
  targetHints:
    backref:
    - location_organization_affiliation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Location.yaml
  templatePointers:
    id: /location/-/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: network
  targetHints:
    backref:
    - network_organization_affiliation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /network/-/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: organization
  targetHints:
    backref:
    - organization_organization_affiliation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /organization/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: participatingOrganization
  targetHints:
    backref:
    - participatingOrganization_organization_affiliation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /participatingOrganization/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: contact_organization
  targetHints:
    backref:
    - extended_contact_detail
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /contact/-/organization/reference
  templateRequired:
  - id
properties:
  _active:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``active``.
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  active:
    element_property: true
    title: Whether this organization affiliation record is in active use
    type: boolean
  code:
    binding_description: The role the participating organization providing services
      to the primary organization.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/organization-role
    binding_version: null
    description: Definition of the role the participatingOrganization plays in the
      association.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Definition of the role the participatingOrganization plays
    type: array
  contact:
    description: The contact details of communication devices available at the participatingOrganization
      relevant to this Affiliation.
    element_property: true
    items:
      $ref: ExtendedContactDetail.yaml
    title: Official contact details at the participatingOrganization relevant to this
      Affiliation
    type: array
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  endpoint:
    backref: endpoint_organization_affiliation
    element_property: true
    enum_reference_types:
    - Endpoint
    items:
      $ref: Reference.yaml
    title: Technical endpoints providing access to services operated for this role
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  healthcareService:
    backref: healthcareService_organization_affiliation
    element_property: true
    enum_reference_types:
    - HealthcareService
    items:
      $ref: Reference.yaml
    title: Healthcare services provided through the role
    type: array
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  identifier:
    element_property: true
    items:
      $ref: Identifier.yaml
    title: Business identifiers that are specific to this role
    type: array
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  location:
    backref: location_organization_affiliation
    element_property: true
    enum_reference_types:
    - Location
    items:
      $ref: Reference.yaml
    title: The location(s) at which the role occurs
    type: array
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  network:
    backref: network_organization_affiliation
    element_property: true
    enum_reference_types:
    - Organization
    items:
      $ref: Reference.yaml
    title: The network in which the participatingOrganization provides the role's
      services (if defined) at the indicated locations (if defined)
    type: array
  organization:
    $ref: Reference.yaml
    backref: organization_organization_affiliation
    description: Organization where the role is available (primary organization/has
      members).
    element_property: true
    enum_reference_types:
    - Organization
    title: Organization where the role is available
  participatingOrganization:
    $ref: Reference.yaml
    backref: participatingOrganization_organization_affiliation
    description: The Participating Organization provides/performs the role(s) defined
      by the code to the Primary Organization (e.g. providing services or is a member
      of).
    element_property: true
    enum_reference_types:
    - Organization
    title: Organization that provides/performs the role (e.g. providing services or
      is a member of)
  period:
    $ref: Period.yaml
    element_property: true
    title: The period during which the participatingOrganization is affiliated with
      the primary organization
  resourceType:
    const: OrganizationAffiliation
    default: OrganizationAffiliation
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  specialty:
    binding_description: Specific specialty associated with the participating organization.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/c80-practice-codes
    binding_version: null
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Specific specialty of the participatingOrganization in the context of the
      role
    type: array
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
title: OrganizationAffiliation
type: object

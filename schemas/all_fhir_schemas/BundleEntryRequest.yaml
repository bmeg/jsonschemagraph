$id: BundleEntryRequest
additionalProperties: false
description: Additional execution information (transaction/batch/history). Additional
  information about how this entry should be processed as part of a transaction or
  batch.  For history, it shows how the entry was processed to create the version
  contained in the entry. [See https://hl7.org/fhir/R5/BundleEntryRequest.html] [See
  https://hl7.org/fhir/R5/BundleEntryRequest.html]
links: []
properties:
  _ifMatch:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``ifMatch``.
  _ifModifiedSince:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``ifModifiedSince``.
  _ifNoneExist:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``ifNoneExist``.
  _ifNoneMatch:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``ifNoneMatch``.
  _method:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``method``.
  _url:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``url``.
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  ifMatch:
    description: Only perform the operation if the Etag value matches. For more information,
      see the API section ["Managing Resource Contention"](http.html#concurrency).
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: For managing update contention
    type: string
  ifModifiedSince:
    description: Only perform the operation if the last updated date matches. See
      the API documentation for ["Conditional Read"](http.html#cread).
    element_property: true
    format: date-time
    title: For managing cache currency
    type: string
  ifNoneExist:
    description: Instruct the server not to perform the create if a specified resource
      already exists. For further information, see the API documentation for ["Conditional
      Create"](http.html#ccreate). This is just the query portion of the URL - what
      follows the "?" (not including the "?").
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: For conditional creates
    type: string
  ifNoneMatch:
    description: If the ETag values match, return a 304 Not Modified status. See the
      API documentation for ["Conditional Read"](http.html#cread).
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: For managing cache validation
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  method:
    binding_description: HTTP verbs (in the HTTP command line). See [HTTP rfc](https://tools.ietf.org/html/rfc7231)
      for details.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/http-verb
    binding_version: 5.0.0
    description: In a transaction or batch, this is the HTTP action to be executed
      for this entry. In a history bundle, this indicates the HTTP action that occurred.
    element_property: true
    element_required: true
    enum_values:
    - GET
    - HEAD
    - POST
    - PUT
    - DELETE
    - PATCH
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: GET | HEAD | POST | PUT | DELETE | PATCH
    type: string
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  resourceType:
    const: BundleEntryRequest
    default: BundleEntryRequest
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  url:
    description: The URL for this entry, relative to the root (the address to which
      the request is posted).
    element_property: true
    element_required: true
    pattern: \S*
    title: URL for HTTP equivalent of this entry
    type: string
title: BundleEntryRequest
type: object

$id: PlanDefinitionActionRelatedAction
additionalProperties: false
description: Relationship to another action. A relationship to another action such
  as "before" or "30-60 minutes after start of". [See https://hl7.org/fhir/R5/PlanDefinitionActionRelatedAction.html]
  [See https://hl7.org/fhir/R5/PlanDefinitionActionRelatedAction.html]
links: []
properties:
  _endRelationship:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``endRelationship``.
  _relationship:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``relationship``.
  _targetId:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``targetId``.
  endRelationship:
    binding_description: Defines the types of relationships between actions.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/action-relationship-type
    binding_version: 5.0.0
    description: The relationship of the end of this action to the related action.
    element_property: true
    enum_values:
    - before
    - before-start
    - before-end
    - concurrent
    - concurrent-with-start
    - concurrent-with-end
    - after
    - after-start
    - after-end
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: before | before-start | before-end | concurrent | concurrent-with-start
      | concurrent-with-end | after | after-start | after-end
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  offsetDuration:
    $ref: Duration.yaml
    description: A duration or range of durations to apply to the relationship. For
      example, 30-60 minutes before.
    element_property: true
    one_of_many: offset
    one_of_many_required: false
    title: Time offset for the relationship
  offsetRange:
    $ref: Range.yaml
    description: A duration or range of durations to apply to the relationship. For
      example, 30-60 minutes before.
    element_property: true
    one_of_many: offset
    one_of_many_required: false
    title: Time offset for the relationship
  relationship:
    binding_description: Defines the types of relationships between actions.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/action-relationship-type
    binding_version: 5.0.0
    description: The relationship of the start of this action to the related action.
    element_property: true
    element_required: true
    enum_values:
    - before
    - before-start
    - before-end
    - concurrent
    - concurrent-with-start
    - concurrent-with-end
    - after
    - after-start
    - after-end
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: before | before-start | before-end | concurrent | concurrent-with-start
      | concurrent-with-end | after | after-start | after-end
    type: string
  resourceType:
    const: PlanDefinitionActionRelatedAction
    default: PlanDefinitionActionRelatedAction
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  targetId:
    description: The element id of the target related action.
    element_property: true
    element_required: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: What action is this related to
    type: string
title: PlanDefinitionActionRelatedAction
type: object

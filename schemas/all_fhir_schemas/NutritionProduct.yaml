$id: NutritionProduct
additionalProperties: false
description: A product used for nutritional purposes (i.e. food or supplement). A
  food or supplement that is consumed by patients. [See https://hl7.org/fhir/R5/NutritionProduct.html]
links:
- href: Organization/{id}
  rel: manufacturer
  targetHints:
    backref:
    - manufacturer_nutrition_product
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /manufacturer/-/reference
  templateRequired:
  - id
- href: Reference/{id}
  rel: knownAllergen_reference
  targetHints:
    backref:
    - codeable_reference
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Reference.yaml
  templatePointers:
    id: /knownAllergen/-/reference/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: note_authorReference_Practitioner
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: note_authorReference_PractitionerRole
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: note_authorReference_Patient
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: note_authorReference_RelatedPerson
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: note_authorReference_Organization
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
properties:
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  category:
    binding_description: Codes identifying classes of nutrition products.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/nutrition-product-category
    binding_version: null
    description: Nutrition products can have different classifications - according
      to its nutritional properties, preparation methods, etc.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Broad product groups or categories used to classify the product, such as
      Legume and Legume Products, Beverages, or Beef Products
    type: array
  characteristic:
    element_property: true
    items:
      $ref: NutritionProductCharacteristic.yaml
    title: Specifies descriptive properties of the nutrition product
    type: array
  code:
    $ref: CodeableConcept.yaml
    binding_description: Codes identifying specific types of nutrition products.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/edible-substance-type
    binding_version: null
    description: The code assigned to the product, for example a USDA NDB number,
      a USDA FDC ID number, or a Langual code.
    element_property: true
    title: A code that can identify the detailed nutrients and ingredients in a specific
      food product
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  ingredient:
    element_property: true
    items:
      $ref: NutritionProductIngredient.yaml
    title: Ingredients contained in this product
    type: array
  instance:
    description: Conveys instance-level information about this product item. One or
      several physical, countable instances or occurrences of the product.
    element_property: true
    items:
      $ref: NutritionProductInstance.yaml
    title: One or several physical instances or occurrences of the nutrition product
    type: array
  knownAllergen:
    backref: knownAllergen_nutrition_product
    binding_description: Codes that identify substances that can be an allergen.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/allergen-class
    binding_version: null
    description: Allergens that are known or suspected to be a part of this nutrition
      product.
    element_property: true
    enum_reference_types:
    - Substance
    items:
      $ref: CodeableReference.yaml
    title: Known or suspected allergens that are a part of this product
    type: array
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  manufacturer:
    backref: manufacturer_nutrition_product
    description: The organisation (manufacturer, representative or legal authorization
      holder) that is responsible for the device.
    element_property: true
    enum_reference_types:
    - Organization
    items:
      $ref: Reference.yaml
    title: Manufacturer, representative or officially responsible for the product
    type: array
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  note:
    element_property: true
    items:
      $ref: Annotation.yaml
    title: Comments made about the product
    type: array
  nutrient:
    element_property: true
    items:
      $ref: NutritionProductNutrient.yaml
    title: The product's nutritional information expressed by the nutrients
    type: array
  resourceType:
    const: NutritionProduct
    default: NutritionProduct
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    binding_description: Codes identifying the lifecycle stage of a product.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/nutritionproduct-status
    binding_version: 5.0.0
    description: The current state of the product.
    element_property: true
    element_required: true
    enum_values:
    - active
    - inactive
    - entered-in-error
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: active | inactive | entered-in-error
    type: string
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
title: NutritionProduct
type: object

$id: InventoryReport
additionalProperties: false
description: A report of inventory or stock items. [See https://hl7.org/fhir/R5/InventoryReport.html]
links:
- href: Practitioner/{id}
  rel: reporter_Practitioner
  targetHints:
    backref:
    - inventory_report
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /reporter/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: reporter_Patient
  targetHints:
    backref:
    - inventory_report
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /reporter/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: reporter_RelatedPerson
  targetHints:
    backref:
    - inventory_report
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /reporter/reference
  templateRequired:
  - id
- href: Device/{id}
  rel: reporter_Device
  targetHints:
    backref:
    - inventory_report
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Device.yaml
  templatePointers:
    id: /reporter/reference
  templateRequired:
  - id
- href: Location/{id}
  rel: inventoryListing_location
  targetHints:
    backref:
    - inventory_report_inventory_listing
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Location.yaml
  templatePointers:
    id: /inventoryListing/-/location/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: note_authorReference_Practitioner
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: note_authorReference_PractitionerRole
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: note_authorReference_Patient
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: note_authorReference_RelatedPerson
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: note_authorReference_Organization
  targetHints:
    backref:
    - annotation
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /note/-/authorReference/reference
  templateRequired:
  - id
properties:
  _countType:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``countType``.
  _implicitRules:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``implicitRules``.
  _language:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``language``.
  _reportedDateTime:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``reportedDateTime``.
  _status:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``status``.
  contained:
    description: These resources do not have an independent existence apart from the
      resource that contains them - they cannot be identified independently, nor can
      they have their own independent transaction scope. This is allowed to be a Parameters
      resource if and only if it is referenced by a resource that provides context/meaning.
    element_property: true
    items:
      $ref: Resource.yaml
    title: Contained, inline Resources
    type: array
  countType:
    binding_description: The type of count.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/inventoryreport-counttype
    binding_version: 5.0.0
    description: Whether the report is about the current inventory count (snapshot)
      or a differential change in inventory (change).
    element_property: true
    element_required: true
    enum_values:
    - snapshot
    - difference
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: snapshot | difference
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource. To make the use of extensions safe
      and managable, there is a strict set of governance applied to the definition
      and use of extensions. Though any implementer can define an extension, there
      is a set of requirements that SHALL be met as part of the definition of the
      extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: The logical id of the resource, as used in the URL for the resource.
      Once assigned, this value never changes.
    element_property: true
    maxLength: 64
    minLength: 1
    pattern: ^[A-Za-z0-9\-.]+$
    title: Logical id of this artifact
    type: string
  identifier:
    description: Business identifier for the InventoryReport.
    element_property: true
    items:
      $ref: Identifier.yaml
    title: Business identifier for the report
    type: array
  implicitRules:
    description: A reference to a set of rules that were followed when the resource
      was constructed, and which must be understood when processing the content. Often,
      this is a reference to an implementation guide that defines the special rules
      along with other profiles etc.
    element_property: true
    pattern: \S*
    title: A set of rules under which this content was created
    type: string
  inventoryListing:
    element_property: true
    items:
      $ref: InventoryReportInventoryListing.yaml
    title: An inventory listing section (grouped by any of the attributes)
    type: array
  language:
    binding_description: IETF language tag for a human language
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/all-languages
    binding_version: 5.0.0
    description: The base language in which the resource is written.
    element_property: true
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: Language of the resource content
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  meta:
    $ref: Meta.yaml
    description: The metadata about the resource. This is content that is maintained
      by the infrastructure. Changes to the content might not always be associated
      with version changes to the resource.
    element_property: true
    title: Metadata about the resource
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the resource and that modifies the understanding
      of the element that contains it and/or the understanding of the containing element's
      descendants. Usually modifier elements provide negation or qualification. To
      make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer is allowed
      to define an extension, there is a set of requirements that SHALL be met as
      part of the definition of the extension. Applications processing a resource
      are required to check for modifier extensions.  Modifier extensions SHALL NOT
      change the meaning of any elements on Resource or DomainResource (including
      cannot change the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored
    type: array
  note:
    element_property: true
    items:
      $ref: Annotation.yaml
    title: A note associated with the InventoryReport
    type: array
  operationType:
    $ref: CodeableConcept.yaml
    description: What type of operation is being performed - addition or subtraction.
    element_property: true
    title: addition | subtraction
  operationTypeReason:
    $ref: CodeableConcept.yaml
    element_property: true
    title: The reason for this count - regular count, ad-hoc count, new arrivals,
      etc
  reportedDateTime:
    element_property: true
    element_required: true
    format: date-time
    title: When the report has been submitted
    type: string
  reporter:
    $ref: Reference.yaml
    backref: inventory_report
    element_property: true
    enum_reference_types:
    - Practitioner
    - Patient
    - RelatedPerson
    - Device
    title: Who submits the report
  reportingPeriod:
    $ref: Period.yaml
    element_property: true
    title: The period the report refers to
  resourceType:
    const: InventoryReport
    default: InventoryReport
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    binding_description: The status of the InventoryReport.
    binding_strength: required
    binding_uri: http://hl7.org/fhir/ValueSet/inventoryreport-status
    binding_version: 5.0.0
    description: The status of the inventory check or notification - whether this
      is draft (e.g. the report is still pending some updates) or active.
    element_property: true
    element_required: true
    enum_values:
    - draft
    - requested
    - active
    - entered-in-error
    pattern: ^[^\s]+(\s[^\s]+)*$
    title: draft | requested | active | entered-in-error
    type: string
  text:
    $ref: Narrative.yaml
    description: A human-readable narrative that contains a summary of the resource
      and can be used to represent the content of the resource to a human. The narrative
      need not encode all the structured data, but is required to contain sufficient
      detail to make it "clinically safe" for a human to just read the narrative.
      Resource definitions may define what content should be represented in the narrative
      to ensure clinical safety.
    element_property: true
    title: Text summary of the resource, for human interpretation
title: InventoryReport
type: object

$id: DeviceAssociationOperation
additionalProperties: false
description: The details about the device when it is in use to describe its operation.
  [See https://hl7.org/fhir/R5/DeviceAssociationOperation.html] [See https://hl7.org/fhir/R5/DeviceAssociationOperation.html]
links:
- href: Patient/{id}
  rel: operator_Patient
  targetHints:
    backref:
    - operator_device_association_operation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /operator/-/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: operator_Practitioner
  targetHints:
    backref:
    - operator_device_association_operation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /operator/-/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: operator_RelatedPerson
  targetHints:
    backref:
    - operator_device_association_operation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /operator/-/reference
  templateRequired:
  - id
properties:
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  operator:
    backref: operator_device_association_operation
    element_property: true
    enum_reference_types:
    - Patient
    - Practitioner
    - RelatedPerson
    items:
      $ref: Reference.yaml
    title: The individual performing the action enabled by the device
    type: array
  period:
    $ref: Period.yaml
    element_property: true
    title: Begin and end dates and times for the device's operation
  resourceType:
    const: DeviceAssociationOperation
    default: DeviceAssociationOperation
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  status:
    $ref: CodeableConcept.yaml
    binding_description: Describes the the status of the association operation.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/deviceassociation-operationstatus
    binding_version: null
    description: Device operational condition corresponding to the association.
    element_property: true
    title: Device operational condition
required:
- status
title: DeviceAssociationOperation
type: object

$id: AccountDiagnosis
additionalProperties: false
description: The list of diagnoses relevant to this account. When using an account
  for billing a specific Encounter the set of diagnoses that are relevant for billing
  are stored here on the account where they are able to be sequenced appropriately
  prior to processing to produce claim(s). [See https://hl7.org/fhir/R5/AccountDiagnosis.html]
  [See https://hl7.org/fhir/R5/AccountDiagnosis.html]
links:
- href: Reference/{id}
  rel: condition_reference
  targetHints:
    backref:
    - codeable_reference
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Reference.yaml
  templatePointers:
    id: /condition/reference/reference
  templateRequired:
  - id
properties:
  _dateOfDiagnosis:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``dateOfDiagnosis``.
  _onAdmission:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``onAdmission``.
  _sequence:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``sequence``.
  condition:
    $ref: CodeableReference.yaml
    backref: account_diagnosis
    binding_description: null
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/condition-code
    binding_version: null
    element_property: true
    enum_reference_types:
    - Condition
    title: The diagnosis relevant to the account
  dateOfDiagnosis:
    description: Ranking of the diagnosis (for each type).
    element_property: true
    format: date-time
    title: Date of the diagnosis (when coded diagnosis)
    type: string
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  onAdmission:
    description: Was the Diagnosis present on Admission in the related Encounter.
    element_property: true
    title: Diagnosis present on Admission
    type: boolean
  packageCode:
    description: The package code can be used to group diagnoses that may be priced
      or delivered as a single product. Such as DRGs.
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Package Code specific for billing
    type: array
  resourceType:
    const: AccountDiagnosis
    default: AccountDiagnosis
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  sequence:
    element_property: true
    exclusiveMinimum: 0
    title: Ranking of the diagnosis (for each type)
    type: integer
  type:
    binding_description: The type of diagnosis this condition represents.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/encounter-diagnosis-use
    binding_version: null
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: "Type that this diagnosis has relevant to the account (e.g. admission,\
      \ billing, discharge \u2026)"
    type: array
required:
- condition
title: AccountDiagnosis
type: object

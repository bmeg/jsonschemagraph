$id: VerificationResultPrimarySource
additionalProperties: false
description: Information about the primary source(s) involved in validation. [See
  https://hl7.org/fhir/R5/VerificationResultPrimarySource.html]
links:
- href: Organization/{id}
  rel: who_Organization
  targetHints:
    backref:
    - verification_result_primary_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /who/reference
  templateRequired:
  - id
- href: Practitioner/{id}
  rel: who_Practitioner
  targetHints:
    backref:
    - verification_result_primary_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /who/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: who_PractitionerRole
  targetHints:
    backref:
    - verification_result_primary_source
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /who/reference
  templateRequired:
  - id
properties:
  _validationDate:
    $ref: FHIRPrimitiveExtension.yaml
    title: Extension field for ``validationDate``.
  canPushUpdates:
    $ref: CodeableConcept.yaml
    binding_description: Ability of the primary source to push updates/alerts.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-can-push-updates
    binding_version: null
    description: Ability of the primary source to push updates/alerts (yes; no; undetermined).
    element_property: true
    title: yes | no | undetermined
  communicationMethod:
    binding_description: Method for communicating with the data source (manual; API;
      Push).
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-communication-method
    binding_version: null
    description: Method for communicating with the primary source (manual; API; Push).
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Method for exchanging information with the primary source
    type: array
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  pushTypeAvailable:
    binding_description: Type of alerts/updates the primary source can send.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-push-type-available
    binding_version: null
    description: Type of alerts/updates the primary source can send (specific requested
      changes; any changes; as defined by source).
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: specific | any | source
    type: array
  resourceType:
    const: VerificationResultPrimarySource
    default: VerificationResultPrimarySource
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  type:
    binding_description: Type of the validation primary source.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-primary-source-type
    binding_version: null
    element_property: true
    items:
      $ref: CodeableConcept.yaml
    title: Type of primary source (License Board; Primary Education; Continuing Education;
      Postal Service; Relationship owner; Registration Authority; legal source; issuing
      source; authoritative source)
    type: array
  validationDate:
    element_property: true
    format: date-time
    title: When the target was validated against the primary source
    type: string
  validationStatus:
    $ref: CodeableConcept.yaml
    binding_description: Status of the validation of the target against the primary
      source.
    binding_strength: preferred
    binding_uri: http://hl7.org/fhir/ValueSet/verificationresult-validation-status
    binding_version: null
    description: Status of the validation of the target against the primary source
      (successful; failed; unknown).
    element_property: true
    title: successful | failed | unknown
  who:
    $ref: Reference.yaml
    backref: verification_result_primary_source
    element_property: true
    enum_reference_types:
    - Organization
    - Practitioner
    - PractitionerRole
    title: Reference to the primary source
title: VerificationResultPrimarySource
type: object

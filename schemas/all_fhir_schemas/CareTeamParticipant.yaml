$id: CareTeamParticipant
additionalProperties: false
description: Members of the team. Identifies all people and organizations who are
  expected to be involved in the care team. [See https://hl7.org/fhir/R5/CareTeamParticipant.html]
  [See https://hl7.org/fhir/R5/CareTeamParticipant.html]
links:
- href: Practitioner/{id}
  rel: member_Practitioner
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Practitioner.yaml
  templatePointers:
    id: /member/reference
  templateRequired:
  - id
- href: PractitionerRole/{id}
  rel: member_PractitionerRole
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: PractitionerRole.yaml
  templatePointers:
    id: /member/reference
  templateRequired:
  - id
- href: RelatedPerson/{id}
  rel: member_RelatedPerson
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: RelatedPerson.yaml
  templatePointers:
    id: /member/reference
  templateRequired:
  - id
- href: Patient/{id}
  rel: member_Patient
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Patient.yaml
  templatePointers:
    id: /member/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: member_Organization
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /member/reference
  templateRequired:
  - id
- href: CareTeam/{id}
  rel: member_CareTeam
  targetHints:
    backref:
    - member_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: CareTeam.yaml
  templatePointers:
    id: /member/reference
  templateRequired:
  - id
- href: Organization/{id}
  rel: onBehalfOf
  targetHints:
    backref:
    - onBehalfOf_care_team_participant
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /onBehalfOf/reference
  templateRequired:
  - id
properties:
  coveragePeriod:
    $ref: Period.yaml
    element_property: true
    one_of_many: coverage
    one_of_many_required: false
    title: When the member is generally available within this care team
  coverageTiming:
    $ref: Timing.yaml
    element_property: true
    one_of_many: coverage
    one_of_many_required: false
    title: When the member is generally available within this care team
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  member:
    $ref: Reference.yaml
    backref: member_care_team_participant
    description: The specific person or organization who is participating/expected
      to participate in the care team.
    element_property: true
    enum_reference_types:
    - Practitioner
    - PractitionerRole
    - RelatedPerson
    - Patient
    - Organization
    - CareTeam
    title: Who is involved
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  onBehalfOf:
    $ref: Reference.yaml
    backref: onBehalfOf_care_team_participant
    description: The organization of the practitioner.
    element_property: true
    enum_reference_types:
    - Organization
    title: Organization of the practitioner
  resourceType:
    const: CareTeamParticipant
    default: CareTeamParticipant
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  role:
    $ref: CodeableConcept.yaml
    binding_description: Indicates specific responsibility of an individual within
      the care team, such as "Primary physician", "Team coordinator", "Caregiver",
      etc.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/participant-role
    binding_version: null
    description: Indicates specific responsibility of an individual within the care
      team, such as "Primary care physician", "Trained social worker counselor", "Caregiver",
      etc.
    element_property: true
    title: Type of involvement
title: CareTeamParticipant
type: object

$id: MedicinalProductDefinitionOperation
additionalProperties: false
description: A manufacturing or administrative process for the medicinal product.
  A manufacturing or administrative process or step associated with (or performed
  on) the medicinal product. [See https://hl7.org/fhir/R5/MedicinalProductDefinitionOperation.html]
links:
- href: Organization/{id}
  rel: organization
  targetHints:
    backref:
    - organization_medicinal_product_definition_operation
    direction:
    - outbound
    multiplicity:
    - has_many
  targetSchema:
    $ref: Organization.yaml
  templatePointers:
    id: /organization/-/reference
  templateRequired:
  - id
- href: Reference/{id}
  rel: type_reference
  targetHints:
    backref:
    - codeable_reference
    direction:
    - outbound
    multiplicity:
    - has_one
  targetSchema:
    $ref: Reference.yaml
  templatePointers:
    id: /type/reference/reference
  templateRequired:
  - id
properties:
  confidentialityIndicator:
    $ref: CodeableConcept.yaml
    binding_description: Confidentiality rating, e.g. commercial sensitivity for a
      Medicinal Product.
    binding_strength: example
    binding_uri: http://hl7.org/fhir/ValueSet/medicinal-product-confidentiality
    binding_version: null
    description: Specifies whether this particular business or manufacturing process
      is considered proprietary or confidential.
    element_property: true
    title: Specifies whether this process is considered proprietary or confidential
  effectiveDate:
    $ref: Period.yaml
    element_property: true
    title: Date range of applicability
  extension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element. To make the use of extensions safe and
      managable, there is a strict set of governance applied to the definition and
      use of extensions. Though any implementer can define an extension, there is
      a set of requirements that SHALL be met as part of the definition of the extension.
    element_property: true
    items:
      $ref: Extension.yaml
    title: Additional content defined by implementations
    type: array
  fhir_comments:
    anyOf:
    - type: string
    - items:
        type: string
      type: array
    element_property: false
    title: Fhir Comments
  id:
    description: Unique id for the element within a resource (for internal references).
      This may be any string value that does not contain spaces.
    element_property: true
    pattern: '[ \r\n\t\S]+'
    title: Unique id for inter-element referencing
    type: string
  links:
    links:
      items:
        $ref: https://json-schema.org/draft/2020-12/links.yaml
      type: array
  modifierExtension:
    description: May be used to represent additional information that is not part
      of the basic definition of the element and that modifies the understanding of
      the element in which it is contained and/or the understanding of the containing
      element's descendants. Usually modifier elements provide negation or qualification.
      To make the use of extensions safe and managable, there is a strict set of governance
      applied to the definition and use of extensions. Though any implementer can
      define an extension, there is a set of requirements that SHALL be met as part
      of the definition of the extension. Applications processing a resource are required
      to check for modifier extensions.  Modifier extensions SHALL NOT change the
      meaning of any elements on Resource or DomainResource (including cannot change
      the meaning of modifierExtension itself).
    element_property: true
    items:
      $ref: Extension.yaml
    title: Extensions that cannot be ignored even if unrecognized
    type: array
  organization:
    backref: organization_medicinal_product_definition_operation
    description: The organization or establishment responsible for (or associated
      with) the particular process or step, examples include the manufacturer, importer,
      agent.
    element_property: true
    enum_reference_types:
    - Organization
    items:
      $ref: Reference.yaml
    title: The organization responsible for the particular process, e.g. the manufacturer
      or importer
    type: array
  resourceType:
    const: MedicinalProductDefinitionOperation
    default: MedicinalProductDefinitionOperation
    description: One of the resource types defined as part of FHIR
    title: Resource Type
    type: string
  type:
    $ref: CodeableReference.yaml
    backref: medicinal_product_definition_operation
    description: The type of manufacturing operation e.g. manufacturing itself, re-packaging.
      For the authorization of this, a RegulatedAuthorization would point to the same
      plan or activity referenced here.
    element_property: true
    enum_reference_types:
    - ActivityDefinition
    - PlanDefinition
    title: The type of manufacturing operation e.g. manufacturing itself, re-packaging
title: MedicinalProductDefinitionOperation
type: object
